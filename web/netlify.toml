[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

# Next.js plugin handles SSR, API routes, and middleware automatically
[[plugins]]
  package = "@netlify/plugin-nextjs"
